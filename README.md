# bbdata-brew

Homebrew tap for [dotf](https://github.com/abjoru/dotf-v3) — a modular dotfile manager using git bare repo with sparse checkout.

## Install

```bash
brew tap abjoru/bbdata-brew
brew install dotf
```

## Upgrade

```bash
brew update
brew upgrade dotf
```

## Uninstall

```bash
brew uninstall dotf
brew untap abjoru/bbdata-brew
```
